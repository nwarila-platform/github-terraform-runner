secure-wazuh:
  description: "STIG- and FIPS-hardened Wazuh 4.14.5 all-in-one SIEM: data-only Terraform provisions, composed Ansible configures, and a commit-to-main GitOps loop drives a permanent Proxmox instance plus an ephemeral AWS proof-of-concept."
  visibility: "public"
  # Codified from the live file so an apply stops trying to replace it with the bare
  # org default. The rule itself is identical to that default; the header is the part
  # worth keeping, and per the drift issue an intentional hand-edit belongs here.
  codeowners: |
    # =============================================================================
    # Code Owners — secure-wazuh
    # =============================================================================
    # The organization team owns every path by default, so GitHub requests a review
    # from @NWarila on any pull request opened against this repository.
    # More specific rules may be added below; for any file the LAST matching pattern
    # wins, so keep the catch-all first and narrow overrides beneath it.
    # =============================================================================

    * @NWarila

  topics:
    - wazuh
    - siem
    - ansible
    - terraform
    - gitops
    - stig
    - fips
    - hardening
    - security
    - compliance
    - devsecops
    - infrastructure-as-code
    - linux
