deploy-application-template:
  description: "Canonical template for deploy-* GitOps deployment repositories: Kustomize base+overlay skeleton, digest-pinned image validation with a parameterized expected_image, repo ADR skeleton, and compatibility with the Talos cluster deploy-* onboarding contract (sync-deploy-repos discovery + deploy-reconciler RBAC)."
  visibility: "public"
  is_template: true
  topics:
    - gitops
    - flux
    - kustomize
    - kubernetes
    - talos
    - template
    - project-template
    - deployment
    - digest-pinning
    - cosign
    - kyverno
    - devsecops
    - platform-engineering
    - supply-chain-security
